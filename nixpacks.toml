# Nixpacks configuration for Railway
# DO NOT USE THIS - Railway should use backend/nixpacks.toml instead
# Set Root Directory to "backend" in Railway dashboard!

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

# Empty build - Railway should NOT build from root
[phases.build]
cmds = []

